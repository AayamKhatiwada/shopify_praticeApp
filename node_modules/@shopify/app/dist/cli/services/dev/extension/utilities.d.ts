import { UIExtension } from '../../../models/app/extensions.js';
/**
 * To prepare Checkout UI Extensions for dev'ing we need to retrieve a valid product variant ID
 * @param extensions - The UI Extensions to dev
 * @param store - The store FQDN
 */
export declare function getCartPathFromExtensions(extensions: UIExtension[], store: string, checkoutCartUrl?: string): Promise<string | undefined>;
/**
 * Returns true if an extension needs a cart URL.
 */
export declare function getExtensionNeedsCartURL(extension: UIExtension): boolean;
