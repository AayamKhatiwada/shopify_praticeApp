import { AppInterface } from '../models/app/app.js';
import { output } from '@shopify/cli-kit';
export declare type Format = 'json' | 'text';
interface InfoOptions {
    format: Format;
    /** When true the command outputs the env. variables necessary to deploy and run web/ */
    webEnv: boolean;
}
export declare function info(app: AppInterface, { format, webEnv }: InfoOptions): Promise<output.Message>;
export declare function infoWeb(app: AppInterface, { format }: Omit<InfoOptions, 'webEnv'>): Promise<output.Message>;
export declare function infoApp(app: AppInterface, { format }: Omit<InfoOptions, 'webEnv'>): Promise<output.Message>;
export {};
