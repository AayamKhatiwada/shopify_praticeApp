import { MatchingError, RemoteSource } from './identifiers.js';
import { IdentifiersExtensions } from '../../models/app/identifiers.js';
import { Result } from '@shopify/cli-kit/common/result';
import type { LocalSource } from './identifiers';
export interface MatchResult {
    identifiers: IdentifiersExtensions;
    toConfirm: {
        local: LocalSource;
        remote: RemoteSource;
    }[];
    toCreate: LocalSource[];
    toManualMatch: {
        local: LocalSource[];
        remote: RemoteSource[];
    };
}
/**
 * Automatically match local sources to remote sources.
 * If we can't match a local source to any remote sources, we can create it.
 * If we are unsure about the matching we can ask the user to confirm the relationship.
 */
export declare function automaticMatchmaking(localSources: LocalSource[], remoteSources: RemoteSource[], identifiers: IdentifiersExtensions, remoteIdField: 'id' | 'uuid'): Promise<Result<MatchResult, MatchingError>>;
