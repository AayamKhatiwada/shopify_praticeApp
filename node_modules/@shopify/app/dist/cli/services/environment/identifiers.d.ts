import { AppInterface } from '../../models/app/app.js';
import { Identifiers, IdentifiersExtensions } from '../../models/app/identifiers.js';
import { ExtensionTypes } from '../../constants.js';
export interface EnsureDeploymentIdsPresenceOptions {
    app: AppInterface;
    token: string;
    appId: string;
    appName: string;
    envIdentifiers: Partial<Identifiers>;
}
export interface RemoteSource {
    uuid: string;
    type: string;
    id: string;
    title: string;
}
export interface LocalSource {
    localIdentifier: string;
    graphQLType: string;
    type: ExtensionTypes;
    configuration: {
        name: string;
    };
}
export declare type MatchingError = 'pending-remote' | 'invalid-environment' | 'user-cancelled';
export declare function ensureDeploymentIdsPresence(options: EnsureDeploymentIdsPresenceOptions): Promise<{
    app: string;
    extensions: {
        [x: string]: string;
    };
    extensionIds: IdentifiersExtensions;
}>;
