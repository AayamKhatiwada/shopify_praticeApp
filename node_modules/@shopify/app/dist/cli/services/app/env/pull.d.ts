import { AppInterface } from '../../../models/app/app.js';
import { output } from '@shopify/cli-kit';
interface PullEnvOptions {
    envFile: string;
}
export declare function pullEnv(app: AppInterface, { envFile }: PullEnvOptions): Promise<output.Message>;
export declare function updateEnvFile(app: AppInterface, envFile: PullEnvOptions['envFile']): Promise<output.Message>;
export {};
