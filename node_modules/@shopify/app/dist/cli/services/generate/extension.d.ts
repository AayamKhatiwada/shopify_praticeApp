import { ExtensionTypes, UIExtensionTypes } from '../../constants.js';
import { AppInterface } from '../../models/app/app.js';
import { DependencyVersion } from '@shopify/cli-kit/node/node-package-manager';
interface ExtensionInitOptions<TExtensionTypes extends ExtensionTypes = ExtensionTypes> {
    name: string;
    extensionType: TExtensionTypes;
    app: AppInterface;
    cloneUrl?: string;
    extensionFlavor?: ExtensionFlavor;
}
export declare type ExtensionFlavor = 'vanilla-js' | 'react' | 'typescript' | 'typescript-react';
interface ExtensionDirectory {
    extensionDirectory: string;
}
declare type UIExtensionInitOptions = ExtensionInitOptions<UIExtensionTypes> & ExtensionDirectory;
declare function extensionInit(options: ExtensionInitOptions): Promise<string>;
export declare function getRuntimeDependencies({ extensionType, extensionFlavor, }: Pick<UIExtensionInitOptions, 'extensionType' | 'extensionFlavor'>): DependencyVersion[];
export default extensionInit;
